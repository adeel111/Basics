<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.adminpannel">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".SelectDriverActivity"
            android:theme="@style/SelectDriverActivityTheme"></activity>
        <activity
            android:name=".DriversActivity"
            android:theme="@style/DriversActivityTheme" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/MainActivityTheme" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service android:name=".Service.ListenOrder" />
    </application>

</manifest>